node_modules/
dist-ssr/
*.local

.DS_Store
*.log
.env*
!.env.example
dist
firebase-applet-config.json

# 예약자 개인정보가 든 작업 파일이 실수로 커밋되지 않도록 저장소 루트의 엑셀/CSV는 무시한다
# (출석부 양식 등 코드에 필요한 파일은 public/sheets/ 아래에 두면 정상적으로 추적된다)
/*.xlsx
/*.csv

# 설치형 에이전트 스킬(third-party). skills-lock.json 으로 재설치되므로 결과물은 추적하지 않는다
.agents/
.claude/skills/
